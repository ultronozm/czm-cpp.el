#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <climits>
#include <cfloat>
#include <vector>
#include <cmath>
#include <set>
#include <list>
#include <map>
#include <stack>
#include <queue>
#include <iostream>
#include <sstream>
#include <fstream>
#include <algorithm>
#include <numeric>
#include <functional>
using namespace std;
#define loop(i,i0,i1) for(int i=(i0);i<(i1);i++)
#define step(i,i0,i1,di) for(int i=(i0);i<(i1);i+=(di))
#define loopi(n) loop(i,0,n)
#define loopj(n) loop(j,0,n)
#define loopij(n) loopi(n) loopj(n)
#define loopijkl(n) loopi(n) loopj(n) loopk(n) loopl(n)
#define loopk(n) loop(k,0,n)
#define loopl(n) loop(l,0,n)
#define X first
#define Y second
#define MP make_pair
#define PB push_back
typedef pair<int,int> PII;
typedef vector<int> VI;

int main() {
}
